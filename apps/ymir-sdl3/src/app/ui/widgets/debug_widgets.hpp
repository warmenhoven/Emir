#pragma once

#include <app/shared_context.hpp>

namespace app::ui::widgets {

// Displays a warning message if debug tracing is disabled with a button to enable it.
void DebugWarning(SharedContext &ctx);

} // namespace app::ui::widgets

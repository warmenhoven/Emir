#ifndef YMIR_VDP_VDP1_ERASE_PARAMS_HLSLI
#define YMIR_VDP_VDP1_ERASE_PARAMS_HLSLI

// See C++ code for documentation on the fields

struct EraseParams {
    uint coords;
    uint writeValue;
    uint vblankEraseParams;
};

#endif
